# PDF documents

Put published PDFs in this directory, then add their details to the `documents` array in `/content.js`.
See the repository's main README for a complete example. There are no sample documents published by default.
