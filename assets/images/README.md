# Photographs

`guanda-wu.jpg` is the main portrait (593 × 724); `eupho-event.jpg` is the second carousel slide (1500 × 1000). Both published copies are optimized and have no EXIF metadata. Keep the full-resolution originals outside this repository.

The carousel displays a consistent 4:5 frame using CSS crops. Update alt text and actual image dimensions in `index.html` when replacing an image; update the Open Graph dimensions when changing the main portrait. See the main README for carousel and replacement instructions.
